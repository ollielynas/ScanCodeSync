use std::path::PathBuf;

use crate::data::data_entry::TimelineEntry;

/// TODO
pub fn get_encoded_time(_path: &PathBuf, _start_time_ms: u64) -> anyhow::Result<Vec<TimelineEntry>> {
    return Ok(vec![]);
}
